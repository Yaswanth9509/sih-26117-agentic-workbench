# Normalize line endings so the repo is identical on Windows, Linux and in Docker.
* text=auto eol=lf

# Shell scripts must keep LF or they break inside the container.
*.sh text eol=lf

# Binary / generated
*.pkl binary
*.faiss binary
